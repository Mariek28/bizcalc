export const metadata = {
  title: 'Contact Us',
  description: 'Get in touch with the SmallBizCalcs team. Report data errors, suggest new calculators, or ask a question.',
};

export default function ContactLayout({ children }) {
  return children;
}
